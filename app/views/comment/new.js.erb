$('#post_<%=@comment.post_id %>').find('.comments-old').append("<%= escape_javascript(render(:partial => 'comment/comments', :object => @comment, :as => "comment")) %>");
/* Reset the comment form */
resetForm($("#formComment"));
exitoComment();
